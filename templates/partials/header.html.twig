<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href=" {{ path('home') }} ">Logo</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('survey') }}">Questionnaires<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="#">Personnalisation<span class="sr-only">(current)</span></a>
            </li>
        </ul>
        {% if app.user %}
            <ul class="navbar-nav">
                <li class="nav-item  active">
                    <a href="{{ path('logout') }}" class="nav-link">Se déconnecter</a>
                </li>
            </ul>
        {% endif %}
        {% if app.user == false %}
        <ul class="navbar-nav">
            <li class="nav-item  active">
                <a href="{{ path('login') }}" class="nav-link">Se connecter</a>
            </li>
        </ul>
    {% endif %}
    </div>
</nav>
