{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form) }}

    </div>
    <div class="form-group d-flex justify-content-center">
        <button class="btn btn-info color_theme">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}

